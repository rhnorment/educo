<section id="hero">

  <%= render partial: 'shared/hero', locals: { heading: @heading, sub_heading: @sub_heading } %>

</section>

<section id="features">

  <%= render 'shared/features' %>

</section>

<section id="pricing">

  <%= render 'shared/reasons' %>

</section>

<section id="slider">

  <%= render 'shared/travel' %>

</section>

<section id="testimonials">

  <%= render 'shared/testimonials' %>

</section>

<section id="cta">

  <%= render 'shared/cta' %>

</section>

<section id="clients">

  <%= render 'shared/partners' %>

</section>
