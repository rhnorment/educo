<section id="hero">
  <div class="container">

    <h1 class="hero-text animated fadeInDown">
      Spend your first 16 months<br /> of college with us
    </h1>

    <div class="cta animated fadeInDown">
      <%= link_to 'Request a free visit', new_visit_path, class:'button button-outline' %>

      <%= link_to 'Apply now', admissions_apply_now_path, class:'button' %>
    </div>

    <%= content_tag :figure, nil, class:'img' %>

   </div>
</section>

<section id="features">

  <%= render 'shared/features' %>

</section>

<section id="pricing">

  <%= render 'shared/reasons' %>

</section>

<section id="slider">

  <%= render 'shared/travel' %>

</section>

<section id="testimonials">

  <%= render 'shared/testimonials' %>

</section>

<section id="cta">
  <p>Take the first step on your 16-month journey of a lifetime!</p>
  <%= link_to 'Apply now', admissions_apply_now_path %>
</section>

<section id="clients">

  <%= render 'shared/partners' %>

</section>
