<p>Click on the course name to view a description.</p>

<div class="table-responsive">

  <table class="table table-boxed table-striped">

    <thead>
        <th>Identifier</th>
        <th>Name</th>
        <th>Term of Study</th>
    </thead>

    <tbody>

        <% @courses.each do |course| %>

            <tr>

              <%= content_tag :td, course.identifier %>

              <%= content_tag :td, link_to(course.name, course, remote:true) %>

              <%= content_tag :td, course.term %>

            </tr>

        <% end %>

    </tbody>

  </table>

</div>

<%= content_tag :div, nil, id:'course-description', class:'modal fade', tabindex:'-1', role:'dialog' %>
